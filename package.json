{
  "name": "crowdmanagement-root",
  "version": "1.0.0",
  "description": "Root package.json for managing frontend",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "start": "npm start --prefix frontend",
    "install:all": "npm install --prefix frontend"
  }
}
